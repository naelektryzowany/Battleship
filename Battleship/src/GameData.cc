#include "GameData.h"

double GameData::getRatio() const {
    return mRatio;
}

int GameData::getSipsLeft() const {
    return mShipsLeft;
}

int GameData::getShipsDown() const {
    return mShipsDown;
}
